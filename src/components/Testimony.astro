---
import apple from "../images/brands/apple.svg";
import quoteicon from "../images/quote.svg";

const review = {
  title: `Ease to use platform just gave us peace of mind`,
  comment: `Deployments used to be all-hands-on-deck nightmares. We'd burn entire days coordinating engineers across teams just to push one feature. Now with FlagPole's feature flags, each squad ships independently. We've gone from weekly deploy parties to a few seamless releases per day.`,
  reviewer: {
    name: `Adriana Naomi`,
    role: `Product Designer`,
    avatar: `https://images.pexels.com/photos/27759912/pexels-photo-27759912/free-photo-of-elegant-glasses-enhancing-the-sophisticated-look.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=2`,
  },
};
---

<section id="customers">
  <div class="container">
    <h1 class="text-lg flex-center">Why developers love FlagPole</h1>
    <div class="testimony-wrapper flex-center gap-2 mt-2">
      <img src={quoteicon.src} alt="quote icon" width="70" />

      <div class="review">
        <div class="reviewer">
          <div class="avatar">
            <img src={review.reviewer.avatar} alt="user avatar" />
          </div>
        </div>
        <div class="review-content mt-2">
          <h1 class="text-lg">"{review.title}</h1>
          <p class="text-sm">{review.comment}"</p>
        </div>
        <div class="review-footer flex gap-1 mt-2">
          <img src={apple.src} alt="apple logo" width="50" />
          <div>
            <p>{review.reviewer.name}</p>
            <p>{review.reviewer.role}</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .testimony-wrapper {
    align-items: baseline;
  }
  .review {
    padding: 40px 60px;
    background: var(--gradient);
    color: var(--dark);
    border-radius: var(--radius-lg);
    border: var(--border);
  }

  .review-content h1 {
    font-weight: 500;
  }

  .review-footer {
    padding-top: 20px;
  }
  .review-footer p {
    margin: 0px;
  }
  .review-footer img {
    background-color: #fff;
  }
  .avatar {
    margin-top: -70px;
  }
</style>
